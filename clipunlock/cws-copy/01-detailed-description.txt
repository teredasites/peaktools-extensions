Enable copy and paste on any website that blocks it. CopyUnlock detects and removes 39 types of copy restrictions instantly — CSS blocks, JavaScript handlers, overlay blockers, clipboard hijacking, debugger traps, print blocking, and right-click disabling. One click. Under 10ms.

800,000+ users just lost their copy/paste extensions to Chrome's Manifest V3 migration. CopyUnlock is built from scratch on MV3 — it's not going anywhere.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

HOW IT WORKS
1. Click the CopyUnlock icon or press Alt+Shift+U
2. CopyUnlock scans the page and detects all active copy restrictions
3. Every restriction is removed — copy, paste, and right-click work normally

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FREE FEATURES (Free Forever — No Trial, No Credit Card)

✓ Enable copy paste on any website — 39 unlock methods across 5 categories
✓ Enable right-click context menu on pages that block it
✓ Unblock text selection on protected web pages
✓ 3 unlock modes: Auto (smart detection), Safe (won't break apps), Aggressive (defeats everything)
✓ Strip hidden watermarks — removes invisible tracking characters from copied text
✓ Normalize homoglyph characters — converts Cyrillic/Greek lookalikes to real Latin text
✓ Neutralize alert dialogs that block right-click
✓ Defeat clipboard hijacking / pastejacking attacks
✓ Bypass debugger traps that freeze DevTools
✓ Unblock Ctrl+P printing and screenshot blocking
✓ Extract text from CSS pseudo-elements (::before/::after)
✓ Extract text from images, background images, and SVG paths
✓ Reverse custom font cipher substitutions
✓ Neutralize transparent overlay blockers
✓ Counter-observer — when sites re-apply copy protection, CopyUnlock fights back automatically
✓ Safe Mode — won't break Google Docs, YouTube, Netflix, Figma, or other web apps
✓ Keyboard shortcuts: Alt+Shift+U (unlock), Alt+Shift+C (clipboard), Alt+Shift+S (search), Ctrl+Shift+V (quick paste)
✓ Clipboard history — 500 items with instant search
✓ 3 collections for organizing copied items
✓ URL auto-citation on every copy
✓ Quick Paste popup (Ctrl+Shift+V) with keyboard navigation
✓ 5 languages: English, Spanish, Portuguese, Chinese, French

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PRO FEATURES

⚡ 100,000 clipboard items with 10-year retention
⚡ 1,000 color-coded collections
⚡ Formatted auto-citation (Title — Domain — Date)
⚡ PDF Line Break Cleanup — fixes broken line breaks and hyphenated words from PDF copies
⚡ Smart Paste in 4 formats: plain, rich, clean, or with citation
⚡ Fuzzy search, tags, and pinned favorites
⚡ Export and import clipboard history (JSON)
⚡ Per-site unlock profiles — save custom settings per domain
⚡ Priority support

Pro: $1.99/month · $19.99/year (save 16%) · $49.99 lifetime

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHY COPYUNLOCK?

Unlike simple copy unlockers that only remove CSS user-select, CopyUnlock detects restrictions across 5 categories:

1. CSS blocks — user-select:none, pointer-events:none, -webkit-touch-callout, ::selection hiding
2. JavaScript handlers — oncopy, onpaste, oncut, oncontextmenu, onselectstart, onkeydown, alert() traps
3. DOM manipulation — MutationObserver re-blockers, overlay divs, inert attributes, Shadow DOM
4. Clipboard API interception — pastejacking, clipboardData.setData hijacking, getSelection override
5. Server-side protections — watermark injection, homoglyph substitution, custom font ciphers, image-based text, SVG path rendering, debugger traps, print blocking

Most copy protection uses 3-5 of these simultaneously. Other extensions only handle 1-2 categories and fail on heavily protected pages. CopyUnlock handles all 39 methods across all 5 categories.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3 UNLOCK MODES

Auto Mode (default) — Scans the page, detects what's blocked, applies targeted fixes. Proactively adds core protections when any restriction is found. Works on ~95% of sites.

Safe Mode — CSS and inline attribute fixes only. Won't patch JavaScript prototypes or intercept events. Use this on web apps where you want minimal interference.

Aggressive Mode — Applies all 18 bypass actions regardless of detection. Patches Function constructor, eval, setInterval, setTimeout. Use this on the most locked-down sites.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BUILT FOR PRIVACY

• Zero data collection — no analytics, no tracking, no telemetry
• No network requests — everything runs locally on your device
• Clipboard history stored in local IndexedDB — never leaves your browser
• No account required — install and use immediately
• Full privacy policy: https://peaktools.dev/privacy/copyunlock

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SAFE MODE — WON'T BREAK YOUR APPS

CopyUnlock automatically detects and protects web apps that need clipboard control:
• Google Docs, Sheets, and Slides
• YouTube
• Netflix and streaming sites
• Figma and design tools
• Online code editors (VS Code Web, CodePen, etc.)

Safe Mode activates automatically. You can also add custom safe domains.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PERFECT FOR

• Students copying text from research sites and academic databases
• Developers copying code snippets from documentation
• Researchers saving content from news articles and journals
• Writers collecting quotes and references
• Anyone tired of right-click being disabled

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

KEYBOARD SHORTCUTS

Alt+Shift+U — Toggle copy/paste unlock on current page
Alt+Shift+C — Open clipboard history panel
Alt+Shift+S — Search clipboard history
Ctrl+Shift+V — Quick Paste popup with keyboard navigation

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SUPPORT

Questions or issues? Contact us:
• Website: https://peaktools.dev/copyunlock
• Support: https://peaktools.dev/contact
• Email: support@peaktools.dev

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CopyUnlock is published by PeakTools Publishing. We build focused, privacy-first browser tools that do one thing exceptionally well. No bloat. No ads. No data harvesting. Just tools that work.